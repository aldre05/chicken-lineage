# Tasks

- [ ] Add `descendant-indexing` spec delta (new capability)
- [ ] Add `metadata-access` spec delta (modified requirements)
- [ ] Update README: architecture diagram, endpoint list, repo structure,
      "Current constraints" section (remove the now-inaccurate line about no
      database — there is one now)
- [ ] Archive: merge deltas into `openspec/specs/`, remove this change folder
